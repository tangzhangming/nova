// Sola 标准库 - 通道已满异常
namespace sola.concurrent

/**
 * 通道已满异常（用于非阻塞操作）
 */
public class ChannelFullException extends ChannelException {
    
    public function __construct(string $message = "channel is full") {
        parent::__construct($message);
    }
}
